# termux
从release下载完install.sh后为其赋予执行权限（或直接bash install.sh）

等待代码跑完后再次进入到proot容器里（默认是Debian也可以自行修改我会在注释中指出）再次从release下载xfce4.sh同上操作（有空我会发KDE的一键安装脚本）

一切跑完后你可以通过verify.sh来进行验证

最后运行start就可以切换到VNCview了（端口默认:1也可以在xfce4.sh中改我会在注释中指出）
